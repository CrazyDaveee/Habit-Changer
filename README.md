# 🌱 习惯改变器 (Habit Changer)

**用随机化减少决策消耗，科学改正坏习惯。**

这是一个纯前端的单页应用，基于行为心理学中**执行功能外部化**的理念，通过**随机决策**帮助你逐步摆脱坏习惯。  
数据 100% 存储在浏览器本地，**绝对不联网**，保护你的隐私。

---

## 🎯 核心理念

- **随机化决策**：把“是否应该做”的决定交给程序，减少意志力消耗。
- **渐进式衰减曲线**：基于伦敦大学学院的 21 天习惯模型，习惯触发概率随天数逐渐下降。
- **日内多次抽签**：针对高频习惯，每天可多次决策，且每次成功避免后概率进一步降低。
- **替代行为池**：抽到“不做”时，随机推荐一个替代行为，并支持自定义权重分配。
- **虚拟陪伴植物**：每次成功避免都会增加好感度，植物从种子长成果实，给予正向反馈。
- **数据看板**：28 天习惯日历、心情热力图、替代行为分布，帮你回顾历程。

---

## 🧰 技术栈

| 技术          | 用途                     |
|---------------|--------------------------|
| HTML5         | 页面结构                 |
| CSS3 (Tailwind 风格) | 治愈系主题样式，支持深色模式 |
| Vanilla JS (ES6 Modules) | 业务逻辑、引擎、算法     |
| Chart.js      | 概率曲线、数据看板图表   |
| localStorage  | 数据持久化（纯本地存储） |

---

## 🚀 快速开始

### 1. 运行项目

本页面已经托管到Github
https://crazydaveee.github.io/Haibit-Changer/

因为使用了 ES6 模块，若本地运行需通过**本地服务器**打开（不能直接双击 `index.html`）。

- **VS Code**：安装 Live Server 扩展，右键 `index.html` → `Open with Live Server`。
- **Python**：在项目根目录执行 `python -m http.server 8000`，浏览器访问 `http://localhost:8000`。
- **Node.js**：执行 `npx serve .`，访问提示的地址。

### 2. 打开页面

首次访问会看到 **Welcome 介绍页**，点击“开始设定我的目标”进入设置向导。

---

## 📝 使用说明

### 第一步：设定目标
- 输入坏习惯名称、当前频率、目标频率（支持 次/天 或 次/周）和目标天数。
- 设置**每次抽签间隔**（默认15分钟）和**每日最大抽签次数**。
- 智能建议会实时提示你目标是否合理。

### 第二步：选择替代行为
- 从预设分类（身体、感官、认知、奖赏类）中勾选，或自定义添加。
- 拖动滑块调整每个替代行为被选中的概率（总和保持100%）。

### 主界面
- **概率曲线**：展示习惯发生的概率随时间下降的趋势，红点标记你当前的位置。
- **抽签按钮**：点击后随机决定“允许发生”或“避免”，若避免则展示替代行为。
- **心情记录**：抽签后可点击表情记录当前心情，并可选填一句话。
- **虚拟植物**：好感度随成功次数增长，植物会从种子长成果实。

### 数据看板
- **习惯日历**：过去 28 天的决策记录、心情和经验变化一览。
- **替代行为分布**：饼图展示你使用过的替代行为比例。

### 设置（⚙️）
- 修改抽签间隔和每日最大次数，保存后即时生效。
- 导出数据为 `.json` 备份，或导入旧数据恢复。
- **AI 曲线生成**：生成一段 Prompt 复制到 ChatGPT 等 AI 工具，获取个性化概率曲线数据后粘贴回应用。

---

## 📱 调试模式

在网址末尾添加 `?debug=true` 即可进入调试模式（例如 `http://localhost:8000?debug=true`）。  
调试面板提供以下功能：
- 前进一天（模拟时间流逝）
- 重置今日抽签（允许重新决策）
- 重置所有数据（从头开始）

> ⚠️ 调试模式仅在开发阶段使用，部署后普通用户不会看到该面板。

---

## 🛡️ 隐私与数据

- 所有抽签记录、心情、好感度等数据均保存在你设备的 `localStorage` 中。
- **不会上传任何信息到服务器**，换设备或清除浏览器数据后数据会丢失。
- 建议定期通过“导出数据”功能备份你的历程。

---

## 📦 部署上线

本项目为纯静态页面，可部署到任何支持静态网站的托管服务：

- **GitHub Pages**：免费、简单，适合开发者。
- **Vercel / Netlify**：自动构建，绑定自定义域名方便。
- **其他**：任何能托管 HTML/CSS/JS 的服务器均可。

---

## 📄 开源协议

MIT License © 2025

---

希望这个小工具能陪伴你温和而坚定地走向更好的自己 🌿